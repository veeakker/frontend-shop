import asyncResource from 'veeakker/utils/async-resource';
import { module, test } from 'qunit';

module('Unit | Utility | async-resource', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = asyncResource();
    assert.ok(result);
  });
});
