import wait from 'veeakker/utils/wait';
import { module, test } from 'qunit';

module('Unit | Utility | wait', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = wait();
    assert.ok(result);
  });
});
