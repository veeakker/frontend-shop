import { action } from '@ember/object';
import { inject as service } from '@ember/service';
import Controller from '@ember/controller';

export default class WebshopAccountRoute extends Controller {
  
  // async getAddress(personId) {
  //   debugger;
  //   const person = await this.store.findRecord('person', personId, {include: "postalAddress"});
  //   return person.postalAddress
  // }
}
