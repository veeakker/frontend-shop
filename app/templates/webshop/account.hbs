{{outlet}}


<div class="products-shop">
  <h1 class="tab-title">Mijn account</h1>

  <Design::Forks @useDarkImage={{true}} />
  <p>Naam: {{this.model.person.firstName}}{{this.model.person.lastName}}</p>
</div>

{{outlet}}
