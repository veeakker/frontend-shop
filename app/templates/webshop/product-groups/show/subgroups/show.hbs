<h1 class="uppercase">Producten in de kijker</h1>
<Design::Forks @useDarkImage={{true}} />

<div class="products">
  {{#each this.model.sortedEnabledProducts as |product|}}
    <ProductCard @product={{product}} />
  {{/each}}
</div>

{{outlet}}
