{{#if (goedgekozen)}}
  {{outlet}}

  <div class="ggz-page-header">
    <h1>Algemene voorwaarden</h1>
  </div>
  <div class="ggz-page">
    <p>Zie <LinkTo @route="conditions">algemene voorwaarden</LinkTo> voor de volledige tekst.</p>
  </div>
{{else}}
  {{outlet}}
{{/if}}