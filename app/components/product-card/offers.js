import Component from '@ember/component';

export default class ProductCardOffersCompnonent extends Component {

    // init() {
    //     super.init(...arguments);

    //     this.allUnits(this.product);
    //     if (this.possibleUnits.length > 0) {
    //         if(this.possibleUnits.includes("c62")) {
    //             this.currentUnit = "c62";
    //         } else {
    //             this.currentUnit = this.possibleUnits[0];
    //         }
    //     }    
    // }
}
